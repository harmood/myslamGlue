# colcon 构建 ament_python 包时的安装路径约定：
# 把 console_scripts 生成的入口脚本装到 lib/<包名>/ 下（而非 Python 默认的
# bin/），这样 ROS 2 才能通过 `ros2 run maze_tools quickstart_log_filter` 找到它。

[develop]
script_dir=$base/lib/maze_tools
[install]
install_scripts=$base/lib/maze_tools