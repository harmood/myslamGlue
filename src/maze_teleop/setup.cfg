# ament_python 包的脚本安装位置约定：
# console_scripts 生成的入口脚本会被放进 lib/maze_teleop 下，
# 使 `ros2 run maze_teleop teleop_keyboard` 能按约定的 lib 目录找到它。
# 缺少本文件时入口脚本会落到 bin/，ros2 run 将无法检索到该可执行文件。
[develop]
script_dir=$base/lib/maze_teleop
[install]
install_scripts=$base/lib/maze_teleop